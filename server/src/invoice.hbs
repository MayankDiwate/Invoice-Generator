<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invoice</title>
    <style>
      body { font-family: Arial, sans-serif; margin: 0; padding: 0; }
      .invoice-container { width: 100%; max-width: 800px; margin: 0 auto;
      padding: 20px; border: 1px solid #ddd; box-shadow: 0 0 10px rgba(0, 0, 0,
      0.1); } .header { display: flex; justify-content: space-between;
      align-items: center; border-bottom: 2px solid #000; padding-bottom: 10px;
      margin-bottom: 20px; } .header img { height: 50px; } .header h1 { margin:
      0; } .invoice-details { width: 100%; border-collapse: collapse; }
      .invoice-details th, .invoice-details td { border: 1px solid #ddd;
      padding: 8px; text-align: left; } .invoice-details th { background-color:
      #f2f2f2; } .totals { margin-top: 20px; text-align: right; } .totals div {
      margin-bottom: 10px; } .totals .grand-total { font-size: 20px;
      font-weight: bold; } .footer { margin-top: 30px; font-size: 12px; color:
      #555; }
    </style>
  </head>
  <body>
    <div class="invoice-container">
      <div style="header">
        <h1>INVOICE GENERATOR</h1>
        <img src="logo.svg" alt="Levitation Infotech Logo" />
      </div>

      <table class="invoice-details">
        <thead>
          <tr>
            <th>Product</th>
            <th>Qty</th>
            <th>Rate</th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          {{#each products as |product|}}
            <tr>
              {{!-- <td>{{product.name}}</td> --}}
              {{!-- <td>{{product.quantity}}</td> --}}
              {{!-- <td>{{product.rate}}</td> --}}
              {{!-- <td>{{(product.total)}}</td> --}}
            </tr>
          {{/each}}
        </tbody>
      </table>

      <div class="totals">
        <div>Total: ₹ {{total}}</div>
        <div>GST: {{gst}}%</div>
        <div class="grand-total">Grand Total: ₹ {{grandTotal}}</div>
      </div>

      <div class="footer">
        <p>Valid until: {{validUntil}}</p>
        <p>
          Terms and Conditions<br />
          we are happy to supply any further information you may need and trust
          that you call on us to fill your order. which will receive our prompt
          and careful attention
        </p>
      </div>
    </div>
  </body>
</html>